<%= form_with(model: autore, local: true) do |form| %>
  <% if autore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(autore.errors.count, "error") %> prohibited this autore from being saved:</h2>

      <ul>
      <% autore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :autore_nome %>
  </div>

  <div class="field">
    <%= form.label :data_nasc %>
    <%= form.text_field :data_nasc, id: :autore_data_nasc, class: 'datepicker' %>
  </div>

  <div class="field">
    <%= form.label :pais_origem %>
    <%= form.text_field :pais_origem, id: :autore_pais_origem %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<script type="text/javascript">
$(document).ready(function(){
  $('.datepicker').datepicker({format: 'dd-mm-yyyy'});
});
</script>
